# AGIfor.me Simple Configuration Template
# Copy this file to .env and customize

# =============================================================================
# MEMORY LOCATION - Super Simple!
# =============================================================================

# Where to store your memories:
# - "project" (default) = stores in ./memories/ folder 
# - "documents" = stores in ~/Documents/memories/
MEMORIES_LOCATION=project

# =============================================================================  
# AI SETTINGS
# =============================================================================

# Ollama connection (usually no need to change)
OLLAMA_HOST=127.0.0.1
OLLAMA_PORT=11434

# =============================================================================
# SERVER CONFIGURATION (Optional - defaults to public server)
# =============================================================================

# Server domain (change this if using your own server)
# Default: your-server.com (replace with your actual server domain)
AGIFORME_SERVER_DOMAIN=your-server.com

# =============================================================================
# BRAINXCHANGE CONFIGURATION (P2P Memory Sharing)
# =============================================================================

# BrainXchange server URL (WebSocket)
# Automatically constructed from AGIFORME_SERVER_DOMAIN
BRAINXCHANGE_SERVER=wss://${AGIFORME_SERVER_DOMAIN}/bx

# Your BrainXchange identity (customize these)
BRAINXCHANGE_EMAIL=your-email@example.com
BRAINXCHANGE_NAME=Your Name

# =============================================================================
# BRAIN PROXY CONFIGURATION (For ChatGPT/External AI Integration)
# =============================================================================

# Enable Brain Proxy for external AI platform access
# Set to true if you want to use ChatGPT Custom GPT or other external platforms
BRAIN_PROXY_ENABLED=false

# Your unique route identifier (use your name or unique ID)
# Example: igor, john_doe, company_team, etc.
BRAIN_PROXY_ROUTE=your_unique_route

# Secure access key (generate a strong password)
# Must be at least 8 characters - this protects access to your memories
BRAIN_PROXY_SECRET=your_secure_brain_key_here

# Brain Proxy server URL (usually auto-constructed from server domain)
# Only change if using a custom setup
# BRAIN_PROXY_URL=wss://${AGIFORME_SERVER_DOMAIN}:8082/bp/connect

# Local MCP server endpoint (rarely needs changing)
# BRAIN_PROXY_LOCAL_MCP_URL=http://localhost:8147/mcp

# =============================================================================
# DEVELOPMENT
# =============================================================================

# Environment mode
NODE_ENV=development

# =============================================================================
# INSTRUCTIONS
# =============================================================================

# BASIC SETUP (Local + Claude Code):
# 1. Copy this file: cp config/env.template .env
# 2. Change MEMORIES_LOCATION if you want memories in ~/Documents/
# 3. Run: npm run bootstrap
# 4. That's it! Use with Claude Code: "magi save content"

# ADVANCED SETUP (+ ChatGPT Custom GPT):
# 1. Follow basic setup above
# 2. Set BRAIN_PROXY_ENABLED=true
# 3. Set your BRAIN_PROXY_ROUTE (unique name)
# 4. Generate strong BRAIN_PROXY_SECRET (like a password)
# 5. Deploy server: cd services/braincloud && ./deploy.sh
# 6. Create Custom GPT with provided config
# 7. See docs/brainproxy/BRAIN_PROXY_SETUP_GUIDE.md for details

# Advanced users can add custom MEMORIES_DIR for any path:
# MEMORIES_DIR=/custom/path/to/memories