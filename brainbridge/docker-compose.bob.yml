version: '3.8'

services:
  brainbridge-bob:
    build: .
    container_name: brainbridge-bob
    volumes:
      # Mount instance-specific memories directory
      - ../memories.bob:/app/memories
      # Mount logs directory
      - ./logs:/app/logs
    environment:
      - NODE_ENV=production
      - MEMORIES_DIR=/app/memories
      - LOG_FILE=/app/logs/brainbridge-bob.log
      - INSTANCE_NAME=bob
      - TRACE_MODE=true
      - OLLAMA_HOST=brainbridge-ollama
      - OLLAMA_PORT=11434
            - NODE_ENV=production
      - MEMORIES_DIR=/app/memories
      - LOG_FILE=/app/logs/brainbridge-bob.log
      - INSTANCE_NAME=bob
      - TRACE_MODE=true
    stdin_open: true
    tty: true
    restart: unless-stopped
    networks:
      - brainbridge-shared-net

networks:
  brainbridge-shared-net:
    external: true
    name: brainbridge_brainbridge-net
