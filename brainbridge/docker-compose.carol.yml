version: '3.8'

services:
  brainbridge-carol:
    build: .
    container_name: brainbridge-carol
    ports:
      - "8149:8147"  # Expose MCP HTTP API for Carol
    volumes:
      # Mount instance-specific memories directory
      - ../memories.carol:/app/memories
      # Mount logs directory
      - ./logs:/app/logs
    environment:
      - NODE_ENV=production
      - MEMORIES_DIR=/app/memories
      - LOG_FILE=/app/logs/brainbridge-carol.log
      - INSTANCE_NAME=carol
      - TRACE_MODE=true
      - OLLAMA_HOST=brainbridge-ollama
      - OLLAMA_PORT=11434
      - MCP_PORT=8147
    restart: unless-stopped
    networks:
      - brainbridge-shared-net

networks:
  brainbridge-shared-net:
    external: true
    name: brainbridge_brainbridge-net
