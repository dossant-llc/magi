/**
 * Memory Path Resolution - DEPRECATED
 * Use magi-paths.ts for new code
 */

// Re-export from centralized magi-paths for backward compatibility
export { 
  getMemoriesPath, 
  getAvailableProfiles, 
  profileExists 
} from './magi-paths';